name: nav2_mission_planner
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.6.0
  provider: ^6.1.1
  ros2_api:
    path: ./lib/plugins/ros2_api
  shared_preferences: ^2.5.3
  wifi_info_plugin_plus:
    path: ./lib/plugins/wifi_info_plugin_plus-2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
